/**
 * State admin — Hub.
 *
 * The districts of this state and every block within them, each with its own
 * counts. Same screen as the district tier's, one level wider.
 */
import AdminHubScreen from '@/features/admin/components/AdminHubScreen';

const Hub = () => <AdminHubScreen tier="state" />;

export default Hub;
